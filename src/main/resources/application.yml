spring:
  application:
    name:
      realtime-ranking-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/realrank?rewriteBatchedStatements=true&profileSQL=false
    username: hong8159
    password: 3342
    hikari:
      maximum-pool-size: 20        # 스레드 수(CPU 코어 수)에 여유분 추가
      minimum-idle: 10
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate.format_sql: true
      dialect: org.hibernate.dialect.MySQL9InnoDBDialect
  data:
    redis:
      host: localhost
      port: 6379
  flyway:
    enabled: true
    baseline-on-migrate: true # 기존 유저는 건너뛰고, 신규 유저는 V1 실행
    baseline-version: 1
    clean-disabled: true
server:
  port: 8082
